{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    <div class="col-lg-6 col-12">
        <div class="card">
            <div class="card-body">
                <div class="alert alert-danger">
                    Attention: les identifiants sont sauvegardés en clair dans la base de donnée, n'utilisez pas cet outil si vous ne tenez absolument pas a ce que l'on trouve votre mot de passe.
                </div>
                {{ form_start(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
